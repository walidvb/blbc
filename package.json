{
  "name": "blbc",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "start": "vapid start .",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "push:types": "npm run push:types --workspace web"
  },
  "dependencies": {
    "rss-parser-browser": "^2.2.2"
  }
}
